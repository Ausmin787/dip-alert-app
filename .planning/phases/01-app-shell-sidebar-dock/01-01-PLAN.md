---
phase: 01-app-shell-sidebar-dock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/App.jsx
autonomous: true
requirements:
  - LAY-01
  - LAY-02
  - LAY-03
  - LAY-04
  - STATE-01
user_setup: []
must_haves:
  truths:
    - "App layout uses a viewport-locked flex container at exactly 100dvh."
    - "A collapsible left-hand navigation dock is present at 64px width by default, displaying navigation icons."
    - "Navigation routes correctly render pages (/ to Dashboard, /watchlist, /alerts, /settings) without floating top headers."
    - "Three-column grid shell is established (64px Sidebar Dock, 320px Middle Feed placeholder, and Right Workspace placeholder)."
  artifacts:
    - "frontend/src/App.jsx"
    - "frontend/src/index.css"
  key_links:
    - "App.jsx imports routing views and renders them inside the index.css layout containers."
---

<objective>
Implement the core app-shell layout container and side navigation dock for the Stripe-Style Split Pane redesign.

Purpose: Restructure the app layout from a scrolling website structure to a native-feeling viewport-contained interface.
Output: Viewport-locked three-column base layout and reactive left navigation dock.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-sidebar-dock/01-UI-SPEC.md
@frontend/src/App.jsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define viewport-locked grid and panel classes in CSS</name>
  <files>frontend/src/index.css</files>
  <read_first>frontend/src/index.css</read_first>
  <action>
    Modify `frontend/src/index.css` to define:
    1. `.app-container`: sets display flex, width 100vw, height 100dvh, overflow hidden, and background to `#070a0e`.
    2. `.sidebar-dock`: sets flex-shrink 0, height 100%, background to `#10161d`, border-right to 1px solid `#263241`, and transitions.
    3. `.middle-feed-pane`: sets width 320px, flex-shrink 0, height 100%, background to `#10161d`, border-right to 1px solid `#263241`, and overflow-y auto.
    4. `.workspace-pane`: sets flex-grow 1, height 100%, background to `#070a0e`, and overflow-y auto.
    Ensure that existing global background settings do not override these container boundaries.
  </action>
  <verify>Check that class definitions exist in frontend/src/index.css.</verify>
  <acceptance_criteria>
    - `frontend/src/index.css` contains definitions for `.app-container`, `.sidebar-dock`, and `.middle-feed-pane`.
  </acceptance_criteria>
  <done>`frontend/src/index.css` has layout classes defined.</done>
</task>

<task type="auto">
  <name>Task 2: Reconstruct App.jsx container and Left Sidebar Dock</name>
  <files>frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx</read_first>
  <action>
    Modify `frontend/src/App.jsx`:
    1. Remove the floating `IslandNav` navigation component and all its dependencies.
    2. Build a new `SidebarDock` component:
       - Displays icons for Overview (Dashboard), Watchlist, Alerts, and Settings.
       - Fits in a 64px width container that expands on hover to 240px displaying route labels, using Framer Motion animations.
       - Renders an active marker highlight (sliding indicator tab) on the active NavLink path.
       - Displays the NSE live/closed dot badge and clock inside the sidebar header/footer.
    3. Re-structure the main layout under the `App` component:
       - Wrap the root in `<div className="app-container">`.
       - Render `<SidebarDock />`.
       - Render a placeholder `<div className="middle-feed-pane">` representing the live asset column.
       - Render a main wrapper `<div className="workspace-pane">` containing page transitions and `<Routes>`.
    4. Update page containers so they don't use the top padding (`pt-28`) previously used to avoid the floating island.
  </action>
  <verify>Run Vite development server or build check to ensure React code compiles.</verify>
  <acceptance_criteria>
    - `frontend/src/App.jsx` does not contain `IslandNav` or `InlineLinks` definitions.
    - App layout structure is wrapped in `app-container` and contains the new `SidebarDock` and route links.
  </acceptance_criteria>
  <done>`App.jsx` renders the side dock and routing layout correctly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run frontend type/compilation check:
  ```bash
  cd frontend
  npm run build
  ```
- Run frontend helpers unit tests:
  ```bash
  cd frontend
  npm test
  ```
</verification>

<success_criteria>
- App displays in a viewport-locked layout container.
- Collapsible sidebar navigation expands on hover and routes links correctly.
- Application builds cleanly without linter or compilation errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-sidebar-dock/01-01-SUMMARY.md`
</output>
