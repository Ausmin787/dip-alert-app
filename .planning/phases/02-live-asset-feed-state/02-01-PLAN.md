---
phase: 02-live-asset-feed-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/AssetContext.jsx
  - frontend/src/App.jsx
autonomous: true
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
  - STATE-02
user_setup: []
must_haves:
  truths:
    - "Middle feed column renders live watchlist assets dynamically from /api/status."
    - "Clicking an asset card in the middle column updates the selected asset state."
    - "The selected asset selection is persisted in localStorage across page refreshes."
    - "Each asset card in the feed renders a self-drawing SVG sparkline of the last 30 closes."
  artifacts:
    - "frontend/src/AssetContext.jsx"
    - "frontend/src/App.jsx"
  key_links:
    - "App.jsx imports AssetContext and consumes its state to render cards and sparklines."
---

<objective>
Implement the dynamic middle asset feed column and persistent state context for selected assets.

Purpose: Connect the feed to real API data, render sparklines and severities, and maintain selection memory.
Output: AssetContext provider and dynamic rendering of watchlist items in the left-center panel.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/App.jsx
@frontend/src/pages/Dashboard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AssetContext provider for unified data fetching</name>
  <files>frontend/src/AssetContext.jsx</files>
  <read_first>frontend/src/api.js, frontend/src/pages/Dashboard.jsx</read_first>
  <action>
    Create a new file `frontend/src/AssetContext.jsx`:
    1. Import React `createContext`, `useContext`, `useEffect`, `useState`.
    2. Import `getStatus`, `getHistory` from `api.js`.
    3. Define `AssetContext = createContext()`.
    4. Build `AssetProvider` component:
       - Maintain `items` state (watchlist assets from `/api/status`).
       - Maintain `selectedAsset` (active ticker string, initialized from `localStorage.getItem('selected_asset')` or defaults to the first active ticker in the list).
       - Maintain `history` state (historical close data for the `selectedAsset` from `/api/history/{ticker}`).
       - Run a polling loop (every 60s) to refresh status data.
       - Hook an effect to fetch history when `selectedAsset` changes, and save the active ticker to `localStorage`.
       - Expose `items`, `selectedAsset`, `setSelectedAsset`, `selectedItem`, `history`, `error`, `loading`, and `refresh` via Context value.
  </action>
  <verify>Ensure the file is created with valid React syntax.</verify>
  <acceptance_criteria>
    - `frontend/src/AssetContext.jsx` exports `AssetContext`, `AssetProvider`, and a custom hook `useAssets`.
  </acceptance_criteria>
  <done>AssetContext file is created and fully defined.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate AssetProvider and build dynamic Middle Feed in App.jsx</name>
  <files>frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx, frontend/src/components/Sparkline.jsx</read_first>
  <action>
    Modify `frontend/src/App.jsx`:
    1. Import `AssetProvider` and `useAssets` hooks.
    2. Wrap the root element in `<AssetProvider>` so the entire app can access the state.
    3. Reconstruct the middle feed panel inside the `App` component to consume context:
       - Fetch `items`, `selectedAsset`, `setSelectedAsset`, `error`, and `loading` from `useAssets`.
       - Map through `items` to render active watchlist assets.
       - Each card is clickable, setting `setSelectedAsset(item.ticker)`.
       - Visually highlight the active card (selected state matches `item.ticker`) using border and background lifts.
       - Fetch 30-day historical closes for the asset (store history array per asset in App or fetch inline) to render the `<Sparkline>` component inside each card.
       - Display the live market clock in the header.
  </action>
  <verify>Ensure the app builds correctly without linter warnings.</verify>
  <acceptance_criteria>
    - `App.jsx` wraps `App` in `AssetProvider`.
    - Middle feed pane maps over `items` from `useAssets()` and renders dynamic data instead of placeholders.
    - Sparkline is imported and rendered on each item card.
  </acceptance_criteria>
  <done>Middle Feed pane is fully dynamic and responsive.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run frontend type/compilation check:
  ```bash
  cd frontend
  npm run build
  ```
- Run unit tests:
  ```bash
  cd frontend
  npm test
  ```
</verification>

<success_criteria>
- Middle feed column updates with real database assets.
- Selection of active ticker persists in localStorage.
- Inter-panel navigation behaves responsively.
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-asset-feed-state/02-01-SUMMARY.md`
</output>
