---
phase: 03-workspace-detail-pane-actions
plan: 01
type: execute
wave: 1
depends_on: [02-live-asset-feed-state]
files_modified:
  - frontend/src/pages/Watchlist.jsx
  - frontend/src/pages/Dashboard.jsx
autonomous: true
requirements:
  - WORKSPACE-01
  - WORKSPACE-02
  - WORKSPACE-03
  - WORKSPACE-04
  - STATE-03
user_setup: []
must_haves:
  truths:
    - "Selecting an asset in the feed dynamically updates metrics in the detail pane."
    - "Watchlist modifications refresh the global middle feed pane instantly."
    - "Redundant rest grids are removed from Dashboard view."
  artifacts:
    - "frontend/src/pages/Watchlist.jsx"
    - "frontend/src/pages/Dashboard.jsx"
  key_links:
    - "Watchlist.jsx calls useAssets().refresh() on CRUD completion."
---

<objective>
Refine right-hand page views to sync watchlist CRUD updates, remove redundant asset lists from Dashboard, and verify token protection and broker buttons.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/pages/Watchlist.jsx
@frontend/src/pages/Dashboard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useAssets refresh sync in Watchlist.jsx</name>
  <files>frontend/src/pages/Watchlist.jsx</files>
  <read_first>frontend/src/pages/Watchlist.jsx</read_first>
  <action>
    Modify `frontend/src/pages/Watchlist.jsx`:
    1. Import `useAssets` from `../AssetContext.jsx`.
    2. Extract `refresh` from `useAssets()` inside the `Watchlist` component.
    3. Invoke `refresh()` inside `save`, `togglePause`, and `remove` methods after completing state reload.
  </action>
  <verify>Ensure compilation and bundling pass.</verify>
  <acceptance_criteria>
    - Middle feed pane refreshes reactively when changes are made inside Watchlist.
  </acceptance_criteria>
  <done>Watchlist updates are integrated with global context state.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Dashboard.jsx to remove redundant asset grids</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <read_first>frontend/src/pages/Dashboard.jsx</read_first>
  <action>
    Modify `frontend/src/pages/Dashboard.jsx`:
    1. Remove the rendering of `rest` assets grid (lines 276-282).
    2. Clean up any unused references.
  </action>
  <verify>Ensure compilation is successful.</verify>
  <acceptance_criteria>
    - Dashboard layout renders without the redundant grid.
  </acceptance_criteria>
  <done>Dashboard layout is streamlined for split-pane UI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run frontend type/compilation check:
  ```bash
  cd frontend
  npm run build
  ```
- Run unit tests:
  ```bash
  cd frontend
  npm test
  ```
</verification>

<success_criteria>
- Watchlist mutations automatically refresh the middle feed.
- Selecting items from the middle feed reactively updates all hero asset metrics and chart in the main panel.
</success_criteria>

<output>
After completion, create `.planning/phases/03-workspace-detail-pane-actions/03-01-SUMMARY.md`
</output>
